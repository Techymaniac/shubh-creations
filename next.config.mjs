/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**", // THIS ALLOWS EVERY IMAGE ON THE INTERNET
      },
    ],
  },
};

export default nextConfig;